{
  "name": "n8n-webhook-proxy",
  "version": "1.0.0",
  "description": "Cloudflare Worker to proxy requests to defined domain with configurable modifications",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:production": "wrangler deploy --env production",
    "deploy:staging": "wrangler deploy --env staging",
    "tail": "wrangler tail",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "cloudflare",
    "worker",
    "proxy",
    "webhook",
    "n8n"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "wrangler": "4.33.1"
  }
}
